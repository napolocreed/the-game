# Optional: URL of the deployed push notification server (see server/README.md).
# Without it, the app still works fully — reminders simply fire only while the
# app (tab or installed PWA) is open.
# Example: VITE_PUSH_SERVER_URL=https://the-game-push-server.onrender.com
VITE_PUSH_SERVER_URL=
